{
  "config": {
    "step": {
      "user": {
        "data": {
          "host": "Host",
          "port": "Port",
          "command_stream": "Command Stream Name"
        },
        "description": "Enter the connection details for your VoiceMeeter host. The Command Stream name must match the 'VBAN Text Incoming Stream' name configured in VoiceMeeter (default is 'Command1')."
      }
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "invalid_auth": "Invalid authentication",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  },
  "entity": {
    "button": {
      "restart_engine": {
        "name": "Restart audio engine"
      },
      "show_window": {
        "name": "Show VM window"
      }
    },
    "switch": {
      "mute": {
        "name": "Mute"
      },
      "solo": {
        "name": "Solo"
      },
      "bus_routing": {
        "name": "Route to {bus}"
      }
    },
    "number": {
      "gain": {
        "name": "Gain"
      }
    },
    "text": {
      "label": {
        "name": "Label"
      }
    }
  },
  "services": {
    "send_raw_command": {
      "name": "Send raw command",
      "description": "Sends a raw text command to the targeted VoiceMeeter device.",
      "fields": {
        "command": {
          "name": "Command",
          "description": "The raw command string (e.g., 'Strip[0].Mute=1;')."
        }
      }
    },
    "set_gain": {
      "name": "Set gain",
      "description": "Set gain for a specific strip or bus number entity.",
      "fields": {
        "gain": {
          "name": "Gain",
          "description": "Gain in dB."
        }
      }
    },
    "set_mute": {
      "name": "Set mute",
      "description": "Set mute state for a specific strip or bus switch entity.",
      "fields": {
        "mute": {
          "name": "Mute",
          "description": "Whether to mute or unmute."
        }
      }
    }
  }
}
